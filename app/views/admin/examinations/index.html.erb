<div class="container mt-5">
  <div class="row">
    <div class="col-md-10 offset-md-1">
      <h5 class="ml-5 mb-5">受診一覧画面</h5>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>受診開始日時</th>
            <th>予約作成日時</th>
            <th>患者ID</th>
            <th>患者氏名</th>
            <th>性別</th>
            <th>受診ステータス</th>
            <th></th>
          </tr>
        </thead>
        <% @examinations.each do |examination| %>
        <tr>
          <td>
            <%= link_to admin_examination_path(examination.id) do %>
              <%= examination.start_time.strftime("%Y/%m/%d %H:%M") %>
            <% end %>
          </td>
          <td><%= examination.created_at.strftime("%Y/%m/%d %H:%M") %></td>
          <td><%= examination.patient.id %></td>
          <td><%= examination.patient.last_name + examination.patient.first_name %></td>
          <td><%= examination.patient.sex %></td>
          <td>
            <%= form_with model: examination, url: admin_examination_path(examination.id), local: true do |f| %>
              <%= f.select :examination_status, examination.examination_statuses.keys %>
              <%= f.submit "変更", class:"btn btn-sm btn-success"%>
            <% end %>
          </td>
        </tr>
        <% end %>
      </table>
      <%= paginate @examinations %>
    </div>
  </div>
</div>
